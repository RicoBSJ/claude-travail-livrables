/// <reference types="vite/client" />
// Déclare les modules non-JS que Vite sait importer (CSS, images, ?raw…).
// Sans ce fichier, `import './index.css'` est une erreur TS2882 : le compilateur
// ne connaît pas ces extensions, c'est le bundler qui les gère.
